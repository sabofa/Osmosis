{
  "name": "osmosis-cli",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "bin": {
    "osmosis": "./bin/osmosis.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "cli-core": "*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.0"
  }
}
